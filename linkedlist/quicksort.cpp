#include <istream>
#include "creatingll.cpp"
using namespace std;
Node *Partition(Node *head, Node *end)
{

   if (head != end || head == NULL || end == head)
      return NULL;
   Node *p = end;
   Node *start = head;
}

int main()
{
   Node *head = NULL;
   head = Insert(head);
   head = Insert(head);
   head = Insert(head);
   head = Insert(head);
   return 0;
}